name: GitHub Workflow

on:
  push:
    branches: [ "main" ]
    paths-ignore:
      - '**/README.md'
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  CI:
    uses: BrunoBrasolin/workflow-template/.github/workflows/net-docker-ci.yml@main
    with:
      image: worker-notification
    secrets: inherit
  
  CD:
    needs: CI
    uses: BrunoBrasolin/workflow-template/.github/workflows/net-docker-cd.yml@main
    with:
      dockerAccount: brunobrasolin
      applicationName: worker-notification
    secrets: inherit
